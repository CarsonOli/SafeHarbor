# Local environment files can contain secrets and must never be committed.
.env
.env.*
!.env.example

# ML model artifacts — generated by running notebooks, not committed to repo.
*.pkl

# .NET build output — never commit these, they are regenerated on every build.
backend/**/bin/
backend/**/obj/
